@model ForgotPasswordViewModel
@{
    ViewData["Title"] = T["ForgotPasswordTitle"].Value;
}

<div class="auth-card form-card">
    <div class="auth-header">
        <h1>@T["ForgotPasswordTitle"]</h1>
        <p>@T["ForgotPasswordDescription"]</p>
    </div>

    <form method="post">
        <div asp-validation-summary="ModelOnly" class="validation"></div>
        <div class="form-row">
            <label asp-for="Email">@T["Email"]</label>
            <div class="input-wrap"><span class="input-icon">✉</span><input class="with-icon" asp-for="Email" autocomplete="username" /></div>
            <span asp-validation-for="Email"></span>
        </div>
        <button class="btn btn-block" type="submit">@T["SendPasswordResetLink"]</button>
    </form>

    <p class="form-footer"><a asp-action="Login">@T["BackToLogin"]</a></p>
</div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
